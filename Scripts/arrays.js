// push and pop
var colors = ["red" , "black", "blue"];
// colors.push(indigo);
// colors.pop();
/* shift and unshift serve the same purpose as pull/pop they just affect
the beginning of the array as opposed to the end */
/* indexof is used to find the position an object has in an array.
colors.indexof("black"); returns [1]*/
/* slice is used to copy/paste pieces of arrays.
var colors2 = colors.slice(0, 1);
This doesnt alter colors.
colors.slice(); copies the list
